# Kinetic Logic Labs

The portfolio lives in [portfolio](portfolio/). Earlier concepts, audit material, and unused source material are in [archive](archive/).

Requires Node.js 24. To run the portfolio locally:

```bash
cd portfolio
npm ci
npm run dev
```

GitHub Pages automation publishes only the portfolio at the domain root on each push to `main`.
